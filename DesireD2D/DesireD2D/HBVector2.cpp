#include "pch.h"

/// template를 사용했다면 구현체가 .h에 같이 있어야함. 
/// 그 이유를 공부해야함. 

/*
template<typename T>
HBVector2<T>::HBVector2()
	: m_x(0), m_y(0)
{

}

template<typename T>
HBVector2<T>::~HBVector2()
{

}

template<typename T>
HBVector2<T> HBVector2<T>::operator*(const Matrix& matrix)
{
	HBVector2<float> _result;

	_result.m_x = m_y * matrix.m_M11 + m_y * matrix.m_M21;
	_result.m_y = m_y * matrix.m_M12 + m_y * matrix.m_M22;

	return _result;
}
*/

